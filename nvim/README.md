# nvim configs

System configs for neovim.
Place it at `<nvim executable folder>/../share/nvim`

## Dependencies

Relies on [vim-plug](https://github.com/junegunn/vim-plug)
On first start make sure to run `PlugInstall`
